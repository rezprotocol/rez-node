# Copy to .env and edit before `docker compose up`.
PG_USER=rez
PG_PASSWORD=change-me-in-production
PG_DB=rez
LB_PORT=8443
# DNS-pinned hostname clients use; nodes advertise this to the WAN.
ADVERTISED_HOST=your.cluster.example
