name: Release npm

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          path: rez-node

      - name: Checkout pinned rez-core
        uses: actions/checkout@v4
        with:
          repository: rezprotocol/rez-core
          path: rez-core
          ref: 89c23c62e3e8eea8efab90e425785c912681135e

      - name: Checkout pinned rez-sdk
        uses: actions/checkout@v4
        with:
          repository: rezprotocol/rez-sdk
          path: rez-sdk
          ref: 2db12a2ae7798e86b8e2635c159ec748db2fb8c3

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: "https://registry.npmjs.org"

      - name: Install
        run: |
          cp rez-node/release/workspace-package.json package.json
          cp rez-node/release/workspace-package-lock.json package-lock.json
          npm ci --ignore-scripts

      - name: Test
        run: npm test
        working-directory: rez-node

      - name: Ensure tag matches package version
        run: node ./scripts/ensure-tag-version.mjs
        working-directory: rez-node

      - name: Ensure runtime dependencies are published first
        run: |
          npm view @rezprotocol/core@0.6.0-rc.10 version
          npm view @rezprotocol/sdk@0.6.0-rc.10 version
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish
        run: npm publish --access public
        working-directory: rez-node
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
